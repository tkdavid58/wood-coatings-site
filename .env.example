# Password required to access /admin and add new products.
# Change this before deploying anywhere the site is publicly reachable.
ADMIN_PASSWORD=changeme

# Optional: override where the SQLite database file is stored.
# Defaults to ./data/wood-coatings.db
# DB_PATH=./data/wood-coatings.db
